# Used by `npm run build` / Vite production mode.
# This value is public — it is bundled into the browser.
# Never put GEMINI_API_KEY, Google credentials, DATABASE_URL, or JWT/SECRET keys here.
VITE_API_BASE_URL=https://api.prism.com/api/v1
